{
  "compilerOptions": {
    "target": "ES2016", // es 버전
    "module": "ESNext", // commonJS는 require, esnext는 import
    "noImplicitAny": true, // any타입이 의도치 않게 발생 할 경우 에러 띄워줌
    "strictNullChecks": true // null, undefined 타입에 이상한 조작(?) 하면 에러 띄워줌
  }
}